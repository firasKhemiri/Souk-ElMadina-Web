<!DOCTYPE html>
<html lang="en">
<head>
    <title>Cart</title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
  <style>
      body {
          font-family: "lato", sans-serif;
      }

      .container {
          max-width: 1000px;
          margin-left: auto;
          margin-right: auto;
          padding-left: 10px;
          padding-right: 10px;
      }

      h2 {
          font-size: 26px;
          margin: 20px 0;
          text-align: center;
      }
      h2 small {
          font-size: 0.5em;
      }

      .responsive-table li {
          border-radius: 3px;
          padding: 25px 30px;
          display: flex;
          justify-content: space-between;
          margin-bottom: 25px;
      }
      .responsive-table .table-header {
          background-color: #95a5a6;
          font-size: 14px;
          text-transform: uppercase;
          letter-spacing: 0.03em;
      }
      .responsive-table .table-row {
          background-color: #ffffff;
          box-shadow: 0px 0px 9px 0px rgba(0, 0, 0, 0.1);
      }
      .responsive-table .col-1 {
          flex-basis: 10%;
      }
      .responsive-table .col-2 {
          flex-basis: 40%;
      }
      .responsive-table .col-3 {
          flex-basis: 25%;
      }
      .responsive-table .col-4 {
          flex-basis: 25%;
      }
      @media all and (max-width: 767px) {
          .responsive-table .table-header {
              display: none;
          }
          .responsive-table li {
              display: block;
          }
          .responsive-table .col {
              flex-basis: 100%;
          }
          .responsive-table .col {
              display: flex;
              padding: 10px 0;
          }
          .responsive-table .col:before {
              color: #6c7a89;
              padding-right: 10px;
              content: attr(data-label);
              flex-basis: 50%;
              text-align: right;
          }
      }

  </style>
</head>
<body>

<div class="container">
    <h2>Liste des articles</h2>
    <ul class="responsive-table">
        <li class="table-header">
            <div class="col col-1">Article</div>
            <div class="col col-2">Prix</div>
            <div class="col col-3">Quantite</div>
            <div class="col col-4">Totale</div>
        </li>
        {% for a in commande.panier.userRecipeAssociations %}
        <li class="table-row">
            <div class="col col-1" data-label="Article">{{ a.article.nom }}</div>
            <div class="col col-2" data-label="Prix">{{ a.article.prix }}</div>
            <div class="col col-3" data-label="Quantite">{{ a.quantite }}</div>
            <div class="col col-4" data-label="Totale">{{ a.article.prix * a.quantite }}</div>
        </li>
        {% endfor %}

    </ul>
</div>
<div class="container">
    <h2>Information de facturation de la commande n : {{ commande.id }}</h2>
    <ul class="responsive-table">
        <li class="table-header">
            <div class="col col-1">Totale san TVA</div>
            <div class="col col-2">A l'ordre de</div>
            <div class="col col-3">Adresse livraison</div>
            <div class="col col-4">Mode de paiement</div>
            <div class="col col-5">Etat</div>
            <div class="col col-6">Totale</div>
        </li>

            <li class="table-row">
                <div class="col col-1" data-label="Totale san TVA">{{ totale }} DT</div>
                <div class="col col-2" data-label="A l'ordre de">{{ user.nom }} {{ user.prenom }}</div>
                <div class="col col-3" data-label="Adresse livraison">{{ commande.adress_liv }}</div>
                <div class="col col-4" data-label="Mode de paiement">{{ commande.meth_paiment }}</div>
                <div class="col col-5" data-label="Etat">{% if commande.etat %} Payee {% else %} Non payee{% endif %}</div>
                <div class="col col-6" data-label="Totale">{{ totale * 1.18 }} DT</div>
            </li>
    </ul>
</div>

</body>
</html>
